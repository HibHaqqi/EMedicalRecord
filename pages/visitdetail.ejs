<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Visit Details</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-blue-100 text-blue-900">
    <header class="bg-blue-500 text-white p-4">
        <div class="container mx-auto">
            <h1 class="text-3xl font-bold">Visit Details</h1>
        </div>
    </header>

    <main class="container mx-auto p-4">
        <section class="bg-white p-6 rounded-lg shadow-md">
            <% if (visitRecords.data.length > 0) { %>
                <h2 class="text-2xl font-bold mb-4">Visit Records for <%= visitRecords.data[0].name %></h2>
                <table class="min-w-full border">
                    <thead>
                        <tr>
                            <th class="py-2 px-4 border">Date Visit</th>
                            <th class="py-2 px-4 border">Symptoms</th>
                            <th class="py-2 px-4 border">Medicine</th>
                            <th class="py-2 px-4 border">Details</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% 
                        // Sort visit records by date create (oldest to newest)
                        const sortedRecords = visitRecords.data.sort((a, b) => new Date(a.createdAt) - new Date(b.createdAt));
                        sortedRecords.forEach(record => { 
                        %>
                            <tr>
                                <td class="py-2 px-4 border"><%= new Date(record.createdAt).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' }) %></td>
                                <td class="py-2 px-4 border"><%= record.symptoms %></td>
                                <td class="py-2 px-4 border"><%= record.medicine %></td>
                                <td class="py-2 px-4 border">
                                    <a href="/api/patients/v1/getedit/<%= record.id %>" class="bg-blue-500 text-white rounded px-4 py-2">Details</a>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <p>No visit records found for this patient.</p>
            <% } %>
        </section>
    </main>

    <footer class="bg-blue-500 text-white p-4 mt-8">
        <p>&copy; 2024 Medical Record Application. All rights reserved.</p>
    </footer>
</body>
</html>